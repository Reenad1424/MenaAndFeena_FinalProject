spring.application.name=MenaAndFeena_FinalProject


spring.datasource.url=jdbc:mysql://localhost:3306/menaandfeena

spring.datasource.username=root

spring.datasource.password=

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect



spring.jpa.show-sql=true

spring.jpa.generate-ddl=true

spring.jpa.hibernate.ddl-auto=update

spring.web.error.include-message=always

spring.web.error.include-stacktrace=always

# ===================================================================
# REAL EMAIL INTEGRATION
# ===================================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
app.support.email=${SUPPORT_EMAIL}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# ===================================================================
# REAL WHATSAPP INTEGRATION
# ===================================================================
ultramsg.instance.id=${ULTRAMSG_INSTANCE_ID}
ultramsg.token=${ULTRAMSG_TOKEN}

# =========================
# OPENAI CONFIG
# =========================

openai.api.url=https://api.openai.com
openai.api.key=${OPENAI_API_KEY}


moyasar.api.key=${MOYASAR_API_KEY}

# Moyasar payment callback URL. Change to your public URL in production.
#app.payment.callback-url=http://localhost:8080/api/v1/payment/callback

# Omit null fields from all JSON responses globally.
spring.jackson.default-property-inclusion=non_null

app.upload.dir=uploads

nominatim.reverse.url=https://nominatim.openstreetmap.org/reverse

jwt.secret=ManaWaFinaSecretKeyManaWaFinaSecretKey2026
jwt.expiration=86400000

app.payment.callback-url=https://habitat-dole-share.ngrok-free.dev/api/v1/payment/callback



